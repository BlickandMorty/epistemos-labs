[package]
name = "scope-rex-admission"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Proof-carrying admission decisions for bounded agent actions."

[lib]
path = "src/lib.rs"

