[package]
name = "eidos-closed-citation"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Deterministic local retrieval with closed-citation validation."

[lib]
path = "src/lib.rs"

